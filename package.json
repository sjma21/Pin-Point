{
  "name": "pinpoint",
  "version": "0.0.1",
  "private": true,
  "bin": {
    "pinpoint": "./packages/mcp-server/dist/cli.js"
  },
  "dependencies": {
    "@pinpoint/mcp-server": "workspace:*"
  },
  "scripts": {
    "dev": "pnpm --filter @pinpoint/demo dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
