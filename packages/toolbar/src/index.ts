// Toolbar package — implementation coming in a future phase.
// Re-export shared types so consumers can import from @pinpoint/toolbar directly.
export type { Annotation, Session, ToolbarSettings, ToolbarState } from '@pinpoint/shared';
