#!/usr/bin/env node
// MCP server + HTTP server — implementation coming in a future phase.

import type { Session } from '@pinpoint/shared';

const _placeholder: Session | null = null;
void _placeholder;

console.log('[pinpoint-server] Placeholder — server implementation coming soon.');
